# dichvucong datasite runtime dependencies.
# New-portal /api/v1 is behind an F5/TSPD WAF -> must be driven by a real
# browser, so Playwright (+ its Chromium) is required.
playwright>=1.50
omegaconf>=2.3,<3.0
# After install: python -m playwright install chromium-headless-shell
