_base: default.yaml

# New national portal (dichvucong.gov.vn /api/v1). Full structured TTHC
# detail, all agencies, public (no VNeID). Pilot with --limit; full run
# is resumable (cached json/<formality_id>.json skip).

scraper:
  qps: 4.0
  record_per_page: 100
